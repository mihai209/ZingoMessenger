# ZingoMessenger

Stack curent:
- Backend: Node.js (Express) + SQLite/MySQL
- Frontend: Next.js (React)

## Quick start

Backend:
```bash
cd backend
yarn install
yarn dev
```

Frontend:
```bash
cd frontend
yarn install
yarn dev
```

Open `http://localhost:3000`.
